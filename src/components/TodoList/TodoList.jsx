const TodoList = () => {
  return (
    <div>
      <input type="text" placeholder="Enter a task" />
      <button>Add Task</button>
      <ul>
        <li>Sample task</li>
      </ul>
    </div>
  );
};

export default TodoList;
